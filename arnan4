<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1.0,maximum-scale=1.0,user-scalable=no,viewport-fit=cover">
    <title>五兽至尊_SOUND_PRO</title>
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
    <style>
        :root { --neon: #00f2ff; --gold: #ffcc00; --pink: #ff0055; --bg: #05080a; }
        * { box-sizing: border-box; -webkit-tap-highlight-color: transparent; }
        body { margin: 0; padding: 0; background: var(--bg); color: #fff; font-family: sans-serif; height: 100vh; width: 100vw; display: flex; flex-direction: column; align-items: center; overflow: hidden; }

        /* 音频开关 */
        .audio-toggle { position: absolute; top: 15px; right: 15px; z-index: 200; width: 40px; height: 40px; background: rgba(0,0,0,0.5); border: 1px solid var(--neon); border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 20px; box-shadow: 0 0 10px var(--neon); }

        .header { width: 100%; height: 22%; display: flex; flex-direction: column; align-items: center; justify-content: center; }
        .bal-card { width: 85%; background: rgba(255,255,255,0.03); border: 1px solid rgba(0,242,255,0.4); border-radius: 20px; padding: 12px; text-align: center; box-shadow: 0 0 20px rgba(0,242,255,0.1); }
        .bal-val { font-size: 38px; font-weight: 900; color: #fff; text-shadow: 0 0 15px var(--neon); }

        .main-game { flex: 1; width: 100%; display: flex; align-items: center; justify-content: center; position: relative; }
        .wheel-box { position: relative; width: 82vw; max-width: 340px; aspect-ratio: 1 / 1; }
        .wheel { width: 100%; height: 100%; border-radius: 50%; border: 10px solid #1a1a1a; background: #111; position: relative; overflow: hidden; transition: transform 5s cubic-bezier(0.15, 0, 0.15, 1); box-shadow: 0 0 50px rgba(0,0,0,0.8); }

        .sector { position: absolute; width: 100%; height: 100%; clip-path: polygon(50% 50%, 100% 20%, 100% 80%); transform-origin: center; display: flex; align-items: center; justify-content: flex-end; padding-right: 12%; }
        .item { transform: rotate(90deg); display: flex; flex-direction: column; align-items: center; }
        .item b { font-size: 28px; }
        .item span { font-size: 10px; font-weight: bold; background: rgba(0,0,0,0.6); padding: 2px 6px; border-radius: 4px; border: 1px solid var(--neon); white-space: nowrap;}

        .pointer { position: absolute; top: -10px; left: 50%; transform: translateX(-50%); width: 0; height: 0; border-left: 18px solid transparent; border-right: 18px solid transparent; border-top: 40px solid var(--neon); z-index: 100; filter: drop-shadow(0 0 10px var(--neon)); }
        .spin-btn { position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); width: 80px; height: 80px; background: #000; border: 4px solid var(--pink); border-radius: 50%; z-index: 110; color: var(--pink); font-size: 20px; font-weight: 900; display: flex; align-items: center; justify-content: center; box-shadow: 0 0 25px var(--pink); }

        .footer { width: 100%; height: 20%; display: flex; flex-direction: column; align-items: center; gap: 10px; }
        .bet-box { display: flex; align-items: center; gap: 20px; background: rgba(255,255,255,0.05); padding: 8px 20px; border-radius: 30px; }
        .btn-adj { width: 40px; height: 40px; border-radius: 50%; border: 1px solid #555; background: #222; color: #fff; font-size: 20px; }
        #msg { color: var(--gold); font-weight: bold; font-size: 18px; text-shadow: 0 0 5px #000; }
    </style>
</head>
<body>

    <div class="audio-toggle" onclick="toggleMute()" id="muteBtn">🔊</div>

    <div class="header">
        <div class="bal-card">
            <div style="font-size:11px; color:var(--neon); letter-spacing:1px;">VIP BALANCE</div>
            <div class="bal-val">🪙 <span id="bal">1,000</span></div>
        </div>
    </div>

    <div class="main-game">
        <div class="wheel-box">
            <div class="pointer"></div>
            <div class="wheel" id="wheel">
                <div class="sector" style="transform:rotate(-90deg); background:rgba(255,215,0,0.1)">
                    <div class="item"><b>👑</b><span>至尊 MAX</span></div>
                </div>
                <div class="sector" style="transform:rotate(-30deg); background:rgba(255,0,0,0.05)">
                    <div class="item"><b>🦁</b><span>威武 LION</span></div>
                </div>
                <div class="sector" style="transform:rotate(30deg); background:rgba(255,165,0,0.1)">
                    <div class="item"><b>🐯</b><span>霸气 TIGER</span></div>
                </div>
                <div class="sector" style="transform:rotate(90deg); background:rgba(0,255,0,0.05)">
                    <div class="item"><b>🦅</b><span>睿智 EAGLE</span></div>
                </div>
                <div class="sector" style="transform:rotate(150deg); background:rgba(147,112,219,0.1)">
                    <div class="item"><b>🐺</b><span>孤狼 WOLF</span></div>
                </div>
                <div class="sector" style="transform:rotate(210deg); background:rgba(240,230,140,0.05)">
                    <div class="item"><b>🐕</b><span>忠诚 DOG</span></div>
                </div>
            </div>
            <div class="spin-btn" onclick="spin()">SPIN</div>
        </div>
    </div>

    <div class="footer">
        <div class="bet-box">
            <button class="btn-adj" onclick="setB(-10)">-</button>
            <div style="font-size:20px; font-weight:bold; width:80px; text-align:center;"><span id="bet" style="color:var(--gold)">1</span></div>
            <button class="btn-adj" onclick="setB(10)">+</button>
        </div>
        <div id="msg">祝您好运连连！</div>
    </div>

    <!-- 隐藏音频源 -->
    <audio id="bgMusic" loop src="https://assets.mixkit.co/music/preview/mixkit-tech-house-vibes-130.mp3">
    <audio id="spinSound" src="https://assets.mixkit.co/sfx/preview/mixkit-fast-sword-whoosh-2325.mp3">
    <audio id="winSound" src="https://assets.mixkit.co/sfx/preview/mixkit-winning-chimes-2015.mp3">

    <script>
        const tg = window.Telegram.WebApp; tg.expand();
        let b = parseFloat(localStorage.getItem('g_v108') || '1000');
        let bet = 1, r = 0, isS = false, isMuted = false;

        const audios = {
            bg: document.getElementById('bgMusic'),
            spin: document.getElementById('spinSound'),
            win: document.getElementById('winSound')
        };
        audios.bg.volume = 0.3;

        function toggleMute() {
            isMuted = !isMuted;
            document.getElementById('muteBtn').innerText = isMuted ? "🔇" : "🔊";
            if(isMuted) audios.bg.pause(); else audios.bg.play().catch(()=>{});
        }

        function sync() {
            document.getElementById('bal').innerText = Math.floor(b).toLocaleString();
            document.getElementById('bet').innerText = bet;
            localStorage.setItem('g_v108', b);
        }

        window.setB = (v) => { 
            if(!isS) { 
                bet = Math.max(1, Math.min(b, bet+v)); sync(); 
                tg.HapticFeedback.selectionChanged();
                if(!isMuted) audios.bg.play().catch(()=>{}); // 用户点击时尝试开启BGM
            } 
        };

        function spin() {
            if(isS || b < bet) return;
            isS = true; b -= bet; sync();

            tg.HapticFeedback.impactOccurred('heavy');
            if(!isMuted) { audios.spin.play(); audios.bg.volume = 0.1; }

            let idx = Math.floor(Math.random()*6);
            r += (3600 + idx*60);
            document.getElementById('wheel').style.transform = `rotate(-${r}deg)`;
            document.getElementById('msg').innerText = "🎰 幸运转动中...";

            setTimeout(() => {
                const multi = [88, 15, 10, 5, 2, 0];
                let win = Math.floor(bet * multi[idx]);
                b += win; sync();

                if(win > 0) {
                    document.getElementById('msg').innerText = "🎊 恭喜！赢取 " + win + " 金币！";
                    if(!isMuted) audios.win.play();
                    tg.HapticFeedback.notificationOccurred('success');
                } else {
                    document.getElementById('msg').innerText = "💔 差点运气，再来一把！";
                }

                if(!isMuted) audios.bg.volume = 0.3;
                isS = false;
            }, 5000);
        }
        sync();
    </script>
</body>
</html>









<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1.0,maximum-scale=1.0,user-scalable=no,viewport-fit=cover">
    <title>五兽至尊_SOUND_ULTRA</title>
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
    <style>
        :root { --neon: #00f2ff; --gold: #ffcc00; --pink: #ff0055; --bg: #05080a; }
        * { box-sizing: border-box; -webkit-tap-highlight-color: transparent; }
        body { margin: 0; background: var(--bg); color: #fff; font-family: sans-serif; height: 100vh; display: flex; flex-direction: column; align-items: center; overflow: hidden; }

        /* 音频开关：增加呼吸灯效果 */
        .audio-toggle { position: absolute; top: 15px; right: 15px; z-index: 200; width: 45px; height: 45px; background: rgba(0,0,0,0.7); border: 2px solid var(--neon); border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 22px; box-shadow: 0 0 15px var(--neon); animation: breathe 2s infinite; }
        @keyframes breathe { 0% { opacity: 0.6; } 50% { opacity: 1; box-shadow: 0 0 25px var(--neon); } 100% { opacity: 0.6; } }

        .header { width: 100%; height: 22%; display: flex; flex-direction: column; align-items: center; justify-content: center; }
        .bal-card { width: 85%; background: rgba(255,255,255,0.03); border: 1.5px solid rgba(0,242,255,0.4); border-radius: 20px; padding: 12px; text-align: center; }
        .bal-val { font-size: 38px; font-weight: 900; color: #fff; text-shadow: 0 0 15px var(--neon); }

        .main-game { flex: 1; width: 100%; display: flex; align-items: center; justify-content: center; position: relative; }
        .wheel-box { position: relative; width: 82vw; max-width: 340px; aspect-ratio: 1 / 1; }
        .wheel { width: 100%; height: 100%; border-radius: 50%; border: 10px solid #1a1a1a; background: #111; position: relative; overflow: hidden; transition: transform 5s cubic-bezier(0.15, 0, 0.15, 1); box-shadow: 0 0 50px #000; }

        .sector { position: absolute; width: 100%; height: 100%; clip-path: polygon(50% 50%, 100% 20%, 100% 80%); transform-origin: center; display: flex; align-items: center; justify-content: flex-end; padding-right: 12%; }
        .item { transform: rotate(90deg); display: flex; flex-direction: column; align-items: center; }
        .item b { font-size: 32px; }
        .item span { font-size: 10px; font-weight: bold; background: rgba(0,0,0,0.6); padding: 2px 6px; border-radius: 4px; border: 1px solid var(--neon); }

        .pointer { position: absolute; top: -10px; left: 50%; transform: translateX(-50%); width: 0; height: 0; border-left: 18px solid transparent; border-right: 18px solid transparent; border-top: 40px solid var(--neon); z-index: 100; }
        .spin-btn { position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); width: 80px; height: 80px; background: #000; border: 4px solid var(--pink); border-radius: 50%; z-index: 110; color: var(--pink); font-size: 20px; font-weight: 900; display: flex; align-items: center; justify-content: center; box-shadow: 0 0 25px var(--pink); }

        .footer { width: 100%; height: 20%; display: flex; flex-direction: column; align-items: center; gap: 10px; }
        .bet-box { display: flex; align-items: center; gap: 20px; background: rgba(255,255,255,0.05); padding: 8px 20px; border-radius: 30px; border: 1px solid #333; }
        .btn-adj { width: 45px; height: 45px; border-radius: 50%; border: 1px solid #555; background: #222; color: #fff; font-size: 22px; }
        #msg { color: var(--gold); font-weight: bold; font-size: 18px; }
    </style>
</head>
<body>

    <!-- 点击这个呼吸灯按钮即可立刻激活所有声音 -->
    <div class="audio-toggle" onclick="initAudio()" id="muteBtn">🔊</div>

    <div class="header">
        <div class="bal-card">
            <div style="font-size:11px; color:var(--neon); letter-spacing:1px;">VIP BALANCE</div>
            <div class="bal-val">🪙 <span id="bal">1,000</span></div>
        </div>
    </div>

    <div class="main-game">
        <div class="wheel-box">
            <div class="pointer"></div>
            <div class="wheel" id="wheel">
                <!-- 扇区内容保持不变 -->
                <div class="sector" style="transform:rotate(-90deg); background:rgba(255,215,0,0.1)"><div class="item"><b>👑</b><span>至尊 MAX</span></div></div>
                <div class="sector" style="transform:rotate(-30deg); background:rgba(255,0,0,0.05)"><div class="item"><b>🦁</b><span>威武 LION</span></div></div>
                <div class="sector" style="transform:rotate(30deg); background:rgba(255,165,0,0.1)"><div class="item"><b>🐯</b><span>霸气 TIGER</span></div></div>
                <div class="sector" style="transform:rotate(90deg); background:rgba(0,255,0,0.05)"><div class="item"><b>🦅</b><span>睿智 EAGLE</span></div></div>
                <div class="sector" style="transform:rotate(150deg); background:rgba(147,112,219,0.1)"><div class="item"><b>🐺</b><span>孤狼 WOLF</span></div></div>
                <div class="sector" style="transform:rotate(210deg); background:rgba(240,230,140,0.05)"><div class="item"><b>🐕</b><span>忠诚 DOG</span></div></div>
            </div>
            <div class="spin-btn" onclick="spin()">SPIN</div>
        </div>
    </div>

    <div class="footer">
        <div class="bet-box">
            <button class="btn-adj" onclick="setB(-10)">-</button>
            <div style="font-size:24px; font-weight:bold; width:80px; text-align:center;"><span id="bet" style="color:var(--gold)">1</span></div>
            <button class="btn-adj" onclick="setB(10)">+</button>
        </div>
        <div id="msg">点击按钮激活音效</div>
    </div>

    <script>
        const tg = window.Telegram.WebApp; tg.expand();
        let b = parseFloat(localStorage.getItem('g_final') || '1000');
        let bet = 1, r = 0, isS = false, audioInited = false;

        // 使用更稳定的外部源链接
        const bgMusic = new Audio('https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3');
        const spinSound = new Audio('https://s3.amazonaws.com/freecodecamp/simonSound1.mp3');
        const winSound = new Audio('https://s3.amazonaws.com/freecodecamp/simonSound4.mp3');

        bgMusic.loop = true; bgMusic.volume = 0.2;

        // 核心：必须通过这个函数点击一次才能激活声音
        function initAudio() {
            if(!audioInited) {
                bgMusic.play().catch(e => console.log("等待交互"));
                audioInited = true;
                document.getElementById('msg').innerText = "音效已激活 🔊";
                document.getElementById('muteBtn').style.animation = "none";
            } else {
                if(bgMusic.paused) { bgMusic.play(); document.getElementById('muteBtn').innerText = "🔊"; }
                else { bgMusic.pause(); document.getElementById('muteBtn').innerText = "🔇"; }
            }
            tg.HapticFeedback.selectionChanged();
        }

        function sync() {
            document.getElementById('bal').innerText = Math.floor(b).toLocaleString();
            document.getElementById('bet').innerText = bet;
            localStorage.setItem('g_final', b);
        }

        window.setB = (v) => { 
            if(!isS) { 
                bet = Math.max(1, Math.min(b, bet+v)); sync(); 
                tg.HapticFeedback.selectionChanged();
                if(!audioInited) initAudio(); // 点击下注时也尝试激活
            } 
        };

        function spin() {
            if(isS || b < bet) return;
            if(!audioInited) initAudio(); // 启动时也尝试激活

            isS = true; b -= bet; sync();
            tg.HapticFeedback.impactOccurred('heavy');
            spinSound.play().catch(()=>{});

            let idx = Math.floor(Math.random()*6);
            r += (3600 + idx*60);
            document.getElementById('wheel').style.transform = `rotate(-${r}deg)`;

            setTimeout(() => {
                const multi = [50, 15, 10, 5, 2, 0];
                let win = Math.floor(bet * multi[idx]);
                b += win; sync();
                if(win > 0) {
                    document.getElementById('msg').innerText = "💰 赢取 " + win + " !";
                    winSound.play().catch(()=>{});
                    tg.HapticFeedback.notificationOccurred('success');
                } else {
                    document.getElementById('msg').innerText = "再试一次！";
                }
                isS = false;
            }, 5000);
        }
        sync();
    </script>
</body>
</html>









<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1.0,maximum-scale=1.0,user-scalable=no,viewport-fit=cover">
    <title>五兽至尊_MAX_BEAST</title>
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
    <style>
        :root { --neon: #00f2ff; --gold: #ffcc00; --pink: #ff0055; --bg: #05080a; }
        * { box-sizing: border-box; -webkit-tap-highlight-color: transparent; }
        body { margin: 0; background: var(--bg); color: #fff; font-family: 'Arial Black', sans-serif; height: 100vh; display: flex; flex-direction: column; align-items: center; overflow: hidden; }

        /* 顶部余额 - 简洁大气 */
        .header { width: 100%; height: 20%; display: flex; align-items: center; justify-content: center; }
        .bal-card { width: 85%; border-bottom: 2px solid var(--neon); padding: 10px; text-align: center; }
        .bal-val { font-size: 46px; font-weight: 900; color: #fff; text-shadow: 0 0 20px var(--neon); letter-spacing: -1px; }

        /* 转盘中心区 - 巨型化处理 */
        .main-game { flex: 1; display: flex; align-items: center; justify-content: center; width: 100%; }
        .wheel-box { position: relative; width: 92vw; max-width: 380px; aspect-ratio: 1/1; }
        .wheel { width: 100%; height: 100%; border-radius: 50%; border: 12px solid #151515; background: #0a0a0a; transition: transform 5s cubic-bezier(0.1, 0, 0.1, 1); position: relative; overflow: hidden; box-shadow: 0 0 60px #000; }

        /* 扇区背景强化 */
        .sector { position: absolute; width: 100%; height: 100%; clip-path: polygon(50% 50%, 100% 16%, 100% 84%); transform-origin: center; display: flex; align-items: center; justify-content: flex-end; padding-right: 8%; }

        /* 核心：霸气巨型图标 */
        .item { transform: rotate(90deg); display: flex; flex-direction: column; align-items: center; justify-content: center; }
        .item b { 
            font-size: 72px; /* 巨型图标尺寸 */
            filter: drop-shadow(0 0 15px rgba(255,255,255,0.6));
            margin-bottom: -5px;
            animation: beast-pulse 2s infinite ease-in-out;
        }
        @keyframes beast-pulse { 0% { transform: scale(1); } 50% { transform: scale(1.1); } 100% { transform: scale(1); } }

        .item span { font-size: 11px; font-weight: 900; color: var(--neon); text-transform: uppercase; letter-spacing: 1px; text-shadow: 0 0 5px #000; }

        /* 指针与按钮 */
        .pointer { position: absolute; top: -18px; left: 50%; transform: translateX(-50%); width: 0; height: 0; border-left: 25px solid transparent; border-right: 25px solid transparent; border-top: 55px solid var(--neon); z-index: 100; filter: drop-shadow(0 0 20px var(--neon)); }
        .spin-btn { position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); width: 95px; height: 95px; background: radial-gradient(#222, #000); border: 5px solid var(--pink); border-radius: 50%; z-index: 110; color: var(--pink); font-size: 26px; font-weight: 900; display: flex; align-items: center; justify-content: center; box-shadow: 0 0 40px var(--pink); text-shadow: 0 0 10px var(--pink); }

        /* 底部控制区 */
        .footer { width: 100%; height: 18%; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 10px; background: linear-gradient(0deg, rgba(0,242,255,0.05) 0%, transparent 100%); }
        .bet-box { display: flex; align-items: center; gap: 40px; }
        .btn-adj { width: 50px; height: 50px; border-radius: 12px; border: 2px solid #444; background: #111; color: #fff; font-size: 28px; font-weight: bold; }
        #msg { color: var(--gold); font-weight: 900; font-size: 22px; text-transform: uppercase; }
    </style>
</head>
<body>

    <div class="header">
        <div class="bal-card"><div class="bal-val">🪙 <span id="bal">1,000</span></div></div>
    </div>

    <div class="main-game">
        <div class="wheel-box">
            <div class="pointer"></div>
            <div id="wheel" class="wheel">
                <div class="sector" style="transform:rotate(-90deg); background:rgba(255,215,0,0.2)"><div class="item"><b>👑</b><span>至尊</span></div></div>
                <div class="sector" style="transform:rotate(-30deg); background:rgba(255,0,0,0.1)"><div class="item"><b>🦁</b><span>狮王</span></div></div>
                <div class="sector" style="transform:rotate(30deg); background:rgba(255,165,0,0.15)"><div class="item"><b>🐯</b><span>猛虎</span></div></div>
                <div class="sector" style="transform:rotate(90deg); background:rgba(0,255,0,0.1)"><div class="item"><b>🦅</b><span>雄鹰</span></div></div>
                <div class="sector" style="transform:rotate(150deg); background:rgba(147,112,219,0.15)"><div class="item"><b>🐺</b><span>恶狼</span></div></div>
                <div class="sector" style="transform:rotate(210deg); background:rgba(240,230,140,0.1)"><div class="item"><b>🐕</b><span>忠犬</span></div></div>
            </div>
            <div class="spin-btn" onclick="spin()">SPIN</div>
        </div>
    </div>

    <div class="footer">
        <div class="bet-box">
            <button class="btn-adj" onclick="setB(-10)">-</button>
            <div style="font-size:32px; font-weight:900; color:var(--gold);"><span id="bet">1</span></div>
            <button class="btn-adj" onclick="setB(10)">+</button>
        </div>
        <div id="msg">READY TO WIN</div>
    </div>

    <script>
        const tg = window.Telegram.WebApp; tg.expand();
        let b = parseFloat(localStorage.getItem('g_max') || '1000');
        let bet = 1, r = 0, isS = false, audioInited = false;

        const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
        function playTone(freq, dur, type='sine', vol=0.1) {
            if(!audioInited) return;
            const osc = audioCtx.createOscillator();
            const g = audioCtx.createGain();
            osc.type = type; osc.frequency.setValueAtTime(freq, audioCtx.currentTime);
            g.gain.setValueAtTime(vol, audioCtx.currentTime);
            g.gain.exponentialRampToValueAtTime(0.0001, audioCtx.currentTime + dur);
            osc.connect(g); g.connect(audioCtx.destination);
            osc.start(); osc.stop(audioCtx.currentTime + dur);
        }

        function unlock() { if(!audioInited) { audioCtx.resume(); audioInited = true; playTone(500, 0.2); } }

        function sync() {
            document.getElementById('bal').innerText = Math.floor(b).toLocaleString();
            document.getElementById('bet').innerText = bet;
            localStorage.setItem('g_max', b);
        }

        window.setB = (v) => { 
            unlock();
            if(!isS) { 
                bet = Math.max(1, Math.min(b, bet+v)); sync(); 
                tg.HapticFeedback.selectionChanged();
                playTone(800, 0.05, 'square', 0.05); 
            } 
        };

        function spin() {
            unlock();
            if(isS || b < bet) return;
            isS = true; b -= bet; sync();
            tg.HapticFeedback.impactOccurred('heavy');

            let tick = setInterval(() => { if(isS) playTone(150, 0.1, 'triangle', 0.05); }, 150);

            let idx = Math.floor(Math.random()*6);
            r += (3600 + idx*60);
            document.getElementById('wheel').style.transform = `rotate(-${r}deg)`;
            document.getElementById('msg').innerText = "TURNING...";

            setTimeout(() => {
                clearInterval(tick);
                const multi = [88, 15, 10, 5, 2, 0];
                let win = Math.floor(bet * multi[idx]);
                b += win; sync();

                if(win > 0) {
                    document.getElementById('msg').innerText = "WIN: " + win + "!";
                    playTone(500, 0.4); setTimeout(()=>playTone(800, 0.6), 150);
                    tg.HapticFeedback.notificationOccurred('success');
                } else {
                    document.getElementById('msg').innerText = "TRY AGAIN";
                    playTone(100, 0.5, 'sawtooth', 0.05);
                }
                isS = false;
            }, 5000);
        }
        sync();
    </script>
</body>
</html>








<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1.0,maximum-scale=1.0,user-scalable=no,viewport-fit=cover">
    <title>五兽至尊_V135_PERFECT</title>
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
    <style>
        :root { --neon: #00f2ff; --gold: #ffcc00; --pink: #ff0055; --bg: #05080a; }
        * { box-sizing: border-box; -webkit-tap-highlight-color: transparent; }
        body { margin: 0; background: var(--bg); color: #fff; font-family: 'Arial Black', sans-serif; height: 100vh; display: flex; flex-direction: column; align-items: center; overflow: hidden; }

        /* 余额区：增加呼吸灯边框 */
        .header { width: 100%; height: 20%; display: flex; align-items: center; justify-content: center; }
        .bal-card { width: 85%; border-bottom: 2px solid var(--neon); padding: 10px; text-align: center; box-shadow: 0 10px 20px -10px var(--neon); }
        .bal-val { font-size: 44px; font-weight: 900; text-shadow: 0 0 20px var(--neon); }

        /* 转盘区：优化比例 */
        .main-game { flex: 1; display: flex; align-items: center; justify-content: center; width: 100%; position: relative; }
        .wheel-box { position: relative; width: 92vw; max-width: 380px; aspect-ratio: 1/1; }

        /* 指针：位置修正 */
        .pointer { position: absolute; top: -15px; left: 50%; transform: translateX(-50%); width: 0; height: 0; border-left: 20px solid transparent; border-right: 20px solid transparent; border-top: 50px solid var(--neon); z-index: 100; filter: drop-shadow(0 0 15px var(--neon)); }

        .wheel { width: 100%; height: 100%; border-radius: 50%; border: 12px solid #1a1a1a; background: #000; transition: transform 5s cubic-bezier(0.1, 0, 0.1, 1); position: relative; overflow: hidden; }

        /* 扇区：增加金属渐变感 */
        .sector { position: absolute; width: 100%; height: 100%; clip-path: polygon(50% 50%, 100% 17%, 100% 83%); transform-origin: center; display: flex; align-items: center; justify-content: flex-end; padding-right: 8%; }

        /* 图案：位置外移，防止被中心按钮遮挡 */
        .item { transform: rotate(90deg) translateY(-20px); display: flex; flex-direction: column; align-items: center; }
        .item b { font-size: 72px; filter: drop-shadow(0 0 15px rgba(255,255,255,0.4)); }

        /* 中心按钮：缩小尺寸，露出更多图案 */
        .spin-btn { position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); width: 80px; height: 80px; background: #000; border: 4px solid var(--pink); border-radius: 50%; z-index: 110; color: var(--pink); font-size: 20px; font-weight: 900; display: flex; align-items: center; justify-content: center; box-shadow: 0 0 25px var(--pink); text-shadow: 0 0 5px var(--pink); }

        .footer { width: 100%; height: 20%; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 15px; background: linear-gradient(0deg, rgba(0,242,255,0.05) 0%, transparent 100%); }
        .bet-box { display: flex; align-items: center; gap: 35px; }
        .btn-adj { width: 50px; height: 50px; border-radius: 12px; border: 2px solid #444; background: #111; color: #fff; font-size: 28px; }
        #msg { color: var(--gold); font-weight: 900; font-size: 22px; }
    </style>
</head>
<body>

    <div class="header">
        <div class="bal-card"><div class="bal-val">🪙 <span id="bal">1,000</span></div></div>
    </div>

    <div class="main-game">
        <div class="wheel-box">
            <div class="pointer"></div>
            <div id="wheel" class="wheel">
                <div class="sector" style="transform:rotate(-90deg); background:radial-gradient(circle at right, rgba(255,215,0,0.3), transparent 70%)"><div class="item"><b>👑</b></div></div>
                <div class="sector" style="transform:rotate(-30deg); background:radial-gradient(circle at right, rgba(255,0,0,0.2), transparent 70%)"><div class="item"><b>🦁</b></div></div>
                <div class="sector" style="transform:rotate(30deg); background:radial-gradient(circle at right, rgba(255,165,0,0.2), transparent 70%)"><div class="item"><b>🐯</b></div></div>
                <div class="sector" style="transform:rotate(90deg); background:radial-gradient(circle at right, rgba(0,255,0,0.2), transparent 70%)"><div class="item"><b>🦅</b></div></div>
                <div class="sector" style="transform:rotate(150deg); background:radial-gradient(circle at right, rgba(147,112,219,0.2), transparent 70%)"><div class="item"><b>🐺</b></div></div>
                <div class="sector" style="transform:rotate(210deg); background:radial-gradient(circle at right, rgba(240,230,140,0.2), transparent 70%)"><div class="item"><b>🐕</b></div></div>
            </div>
            <div class="spin-btn" onclick="spin()">SPIN</div>
        </div>
    </div>

    <div class="footer">
        <div class="bet-box">
            <button class="btn-adj" onclick="setB(-10)">-</button>
            <div style="font-size:32px; font-weight:900; color:var(--gold);"><span id="bet">1</span></div>
            <button class="btn-adj" onclick="setB(10)">+</button>
        </div>
        <div id="msg">READY TO WIN</div>
    </div>

    <script>
        const tg = window.Telegram.WebApp; tg.expand();
        let b = parseFloat(localStorage.getItem('g_v135') || '1000');
        let bet = 1, r = 0, isS = false, audioCtx;

        function initAudio() {
            if(!audioCtx) {
                audioCtx = new (window.AudioContext || window.webkitAudioContext)();
                // 启动一个低频背景音乐
                const osc = audioCtx.createOscillator();
                const gain = audioCtx.createGain();
                osc.type = 'triangle'; osc.frequency.setValueAtTime(55, audioCtx.currentTime);
                gain.gain.setValueAtTime(0.02, audioCtx.currentTime);
                osc.connect(gain); gain.connect(audioCtx.destination);
                osc.start();
            }
        }

        function sfx(f, d, t='sine', v=0.1) {
            if(!audioCtx) return;
            const o = audioCtx.createOscillator(); const g = audioCtx.createGain();
            o.type = t; o.frequency.setValueAtTime(f, audioCtx.currentTime);
            g.gain.setValueAtTime(v, audioCtx.currentTime);
            g.gain.exponentialRampToValueAtTime(0.0001, audioCtx.currentTime + d);
            o.connect(g); g.connect(audioCtx.destination);
            o.start(); o.stop(audioCtx.currentTime + d);
        }

        function sync() {
            document.getElementById('bal').innerText = Math.floor(b).toLocaleString();
            document.getElementById('bet').innerText = bet;
            localStorage.setItem('g_v135', b);
        }

        window.setB = (v) => { 
            initAudio();
            if(!isS) { 
                bet = Math.max(1, Math.min(b, bet+v)); sync(); 
                tg.HapticFeedback.selectionChanged();
                sfx(600, 0.1, 'square', 0.03);
            } 
        };

        function spin() {
            initAudio();
            if(isS || b < bet) return;
            isS = true; b -= bet; sync();
            tg.HapticFeedback.impactOccurred('heavy');

            let tick = setInterval(() => { if(isS) sfx(150, 0.05, 'triangle', 0.05); }, 150);

            let idx = Math.floor(Math.random()*6);
            r += (3600 + idx*60);
            document.getElementById('wheel').style.transform = `rotate(-${r}deg)`;
            document.getElementById('msg').innerText = "SPINNING...";

            setTimeout(() => {
                clearInterval(tick);
                const multi = [88, 15, 10, 5, 2, 0];
                let win = Math.floor(bet * multi[idx]);
                b += win; sync();

                if(win > 0) {
                    document.getElementById('msg').innerText = "WIN: " + win + "!";
                    sfx(523, 0.4); setTimeout(()=>sfx(659, 0.4), 100);
                    tg.HapticFeedback.notificationOccurred('success');
                } else {
                    document.getElementById('msg').innerText = "LOSE";
                    sfx(100, 0.4, 'sawtooth', 0.05);
                }
                isS = false;
            }, 5000);
        }
        sync();
    </script>
</body>
</html>
